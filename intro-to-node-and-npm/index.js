const fs = require('fs');
const chalk = require('chalk-animation');
  // Named import

const animateString = require('./animate-string');
console.log(animateString)

// const contents = fs.readdirSync('./animals');

// animateString(contents.join('\n'));
